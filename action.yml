name: 'Commit check using commitizen'
description: 'Create a commit check'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'git-commit'
  color: 'purple'
inputs:
  branch:
    description: 'Destination branch to push changes'
    required: false
    default: 'master'
